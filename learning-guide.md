# lp-python 工业级学习指引与全景成长路径

欢迎来到现代 Python 工业级后端核心架构与实战教程。本教程面向 2026 最新行业实践（Python 3.12+、Free-threaded 无 GIL 演进、FastAPI 异步生态、SQLAlchemy 2.0 异步持久层与 AI 原语），兼顾 **广度全面**、**零基础平滑入门** 与 **大厂就职突破**。

---

## 🎯 针对不同工程师的角色学习路线

### 1. 零基础 / 跨语言转栈工程师（从零极速入门到生产实战）
- **推荐路径**：
  `00 工具链效能` $\to$ `01 语法基础与核心范式` $\to$ `02 对象模型与内存` $\to$ `06 Pydantic V2` $\to$ `08 FastAPI 核心`
- **核心目标**：
  - 掌握 `uv` 极速包管理与声明式 `pyproject.toml` 工作流；
  - 彻底掌握基础数据类型、Decimal 金融精度、短路逻辑、for-else 优雅控制流与核心容器推导式；
  - 规避默认可变参数、深浅拷贝等初学者常踩深坑；
  - 掌握现代 FastAPI + Pydantic 开发企业级异步 Web 接口。

### 2. 准备大厂中高级面试（直击八股底层、机制解密与架构压轴）
- **推荐路径**：
  重点攻关全量 38 个专题目录下的 `QA.md`，深挖以下核心考点：
  - **基础与类型**：`01-01` 动态强类型本质与 Decimal 选型、`01-02` is vs == 与短路机制、`01-04` 可变参数默认值陷阱；
  - **内存与 GC**：`02-01` PyObject 结构体模型、`02-04` 引用计数与分代回收三色标记、弱引用打破环；
  - **控制流与元编程**：`03-03` 迭代器协议底层、`04-01` `__new__` vs `__init__`、`04-03` 描述符协议与 ORM 映射原理、`04-05` MRO C3 线性化算法；
  - **并发与锁**：`05-01` GIL 的本质与 Free-threaded 演进、`05-03` TaskGroup 结构化并发、`05-04` 异步信号量限流；
  - **持久层与高可用**：`07-02` SQLAlchemy 异步连接池泄漏防范、`07-03` 缓存穿透/击穿/雪崩治理；
  - **架构设计与排障**：`08-02` FastAPI 依赖注入原理、`09-03` SSE 协议长连接与生产高并发采样 SOP。

### 3. AI 原生全栈架构师（大模型应用、Agent 工具调用与流式交付）
- **推荐路径**：
  `05 异步并发` $\to$ `06 Pydantic 契约` $\to$ `08-02 Depends 依赖注入` $\to$ `09 AI 原语与 MCP`
- **核心目标**：
  - 掌握纯原生向量检索 RAG 管道（纯数学向量余弦相似度计算，剥离第三方重度黑盒依赖）；
  - 掌握大模型 Tool Calling 与 MCP (Model Context Protocol) 传输协议标准；
  - 掌握 SSE 流式打字机响应与异步长连接生命周期优雅管控。

---

## 💡 三位一体学习法

每个专题文件夹都按照严格的工业标准组织：
1. **先读 `README.md`**：通过“工程三段式”（机制原理 $\to$ 避坑指南 $\to$ 示例解析）建立透彻的底层心智模型；
2. **手跑 `main.py`**：每个代码文件严格控制在 **50~80 行**，核心逻辑纯粹自洽，运行 `uv run python main.py` 即刻见效；
3. **精背 `QA.md`**：结合“🎯 面试官考点 + 💡 满分回答模板”，直击大厂考核本质。

---

## 🛠️ 常用验证命令速查

```bash
make help      # 查看所有可用指令
make sync      # 安装依赖并同步虚拟环境
make lint      # 执行 Ruff 静态代码检查
make fmt       # 自动格式化代码
make test      # 运行自动化测试
make run-all   # 验证执行全量 38 个主程序
```
